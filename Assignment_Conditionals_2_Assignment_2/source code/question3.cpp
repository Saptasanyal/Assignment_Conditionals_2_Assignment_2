// inside the if we are comparing between two different datatype, one is int and
//the other one is float. but in c++ the int datatype will be promotated to float 
//and then will be compared to float. as 3.0==3.0 is true so the output is:
//x and y are equal